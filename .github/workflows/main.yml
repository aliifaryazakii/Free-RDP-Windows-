name: RDP-Aliif-Final-NoNamePW

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: 🛠️ 1. Optimasi RDP & Performa
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          $visualPath = "HKCU:\Control Panel\Desktop"
          Set-ItemProperty -Path $visualPath -Name "Wallpaper" -Value ""
          Set-ItemProperty -Path "HKCU:\Software\Microsoft\Windows\CurrentVersion\Explorer\VisualEffects" -Name "VisualFXSetting" -Value 2
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389

      - name: 👤 2. Buat User aliifaryazakii
        run: |
          $userName = "aliifaryazakii"
          
          # PASSWORD SUPER SULIT (Tanpa Nama, Simbol Lengkap, Pasti Lolos Windows)
          $password = "P@ss-$$#ad-#&_99!"
          
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          
          # Hapus user lama kalau ada biar GAK ERROR
          if (Get-LocalUser -Name $userName -ErrorAction SilentlyContinue) {
              Remove-LocalUser -Name $userName
          }
          
          try {
              New-LocalUser -Name $userName -Password $securePass -FullName "Aliif Arya Zakii" -Description "RDP Admin" -PasswordNeverExpires -ErrorAction Stop
              Add-LocalGroupMember -Group "Administrators" -Member $userName
              Add-LocalGroupMember -Group "Remote Desktop Users" -Member $userName
              Write-Host "✅ BERHASIL! User sudah siap tempel."
          } catch {
              Write-Error "❌ Gagal: $_"
              exit 1
          }
          
          echo "RDP_USER=$userName" >> $env:GITHUB_ENV
          echo "RDP_PASS=$password" >> $env:GITHUB_ENV

      - name: 🌐 3. Koneksi Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=aliif-rdp
          for ($i=1; $i -le 10; $i++) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              if ($tsIP) { echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV; break }
              Start-Sleep -Seconds 5
          }

      - name: 🚀 4. Info Login Final
        run: |
          Write-Host "`n" + ("="*45)
          Write-Host "   🚀 RDP SERVER SIAP - ALIIF 🚀"
          Write-Host ("="*45)
          Write-Host "🌐 IP Address : $env:TAILSCALE_IP"
          Write-Host "👤 Username   : $env:RDP_USER"
          Write-Host "🔑 Password   : $env:RDP_PASS"
          Write-Host ("="*45)
          while ($true) {
              Write-Host "Server Aktif - [$(Get-Date -Format 'HH:mm:ss')]"
              Start-Sleep -Seconds 300
          }
